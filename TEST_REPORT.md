# Champion Coach v0.6.1 Repository Edition 検証報告

## 完成条件

今回の完成条件は次の4点です。

1. 提案されたポケモンを持っていなくても構築作成を継続できる
2. 未所持を登録したポケモンを以後の自動候補へ出さない
3. 6体確定後、Champions準拠の性格・能力ポイント・持ち物・技候補を確認できる
4. v0.5までの構築・対戦ログを壊さない

作業は「現状確認 → 原因分析 → 実装 → 静的検証 → 自動テスト → ケース検証 → 修正 → 全件再検証」で繰り返しました。

## 自動テスト結果

### Node 単体テスト

**100 / 100 PASS**

v0.5の83項目をすべて維持し、v0.6向け17項目を追加しました。

追加した主な検証:

- 全258体 × シングル/ダブルの能力ポイント生成
- 全516ケースで各能力0～32
- 全516ケースで合計66
- 生成される性格が許可したスターター性格の範囲内
- ガオガエン等の耐久支援へ低速という理由だけで素早さ下降性格を強制しない
- ブリムオン等、トリックルーム根拠がある場合の低速性格
- 未所持のID/日本語名を正規化
- 未所持を仲間候補から除外
- 未所持をおまかせ6体から除外
- 好きな軸そのものが未所持なら、勝手に別ポケモンへ置換しない
- 役割維持型の代替候補生成
- 代替候補に理由が存在
- 内蔵Reg M-Cダブルデータに技・持ち物が存在
- 数字だけの未解決アイテム名をUIへ出さない
- ダブル育成スターターが公開M-Cデータと能力ポイントを組み合わせる
- シングルへダブルの技データを誤流用しない
- 6体の持ち物重複回避
- 全258の自動生成ダブル構築で持ち物重複なし

## 総当たり検証

### 構築生成

全258エントリを1体ずつ「好きな軸」として、

- ダブル 258通り
- シングル 258通り

を生成しています。

全ケースで:

- 6体完成
- 好きな軸を保持
- 同種族別フォームの重複なし
- 構築バリデーション0件

を確認しました。

### 能力ポイント

全258体をシングル・ダブルの両方で生成し、**516配分すべて**について:

- 各能力 0～32
- 合計 66

を確認しました。

従来作品の数値表記はアプリUI・ロジックへ使用していません。

### 持ち物

全258体を軸に作るダブル構築について、育成スターター6体分を生成しました。

第一候補が重複した場合は次候補へ切り替え、検証対象では**同一構築内の持ち物重複0件**でした。

## 実行時スモークテスト

模擬DOM上で実際の `pokemon-data.js → competitive-data.js → core.js → app.js` を実行しています。

検証シナリオ:

1. v0.5形式の保存データを事前投入
2. 起動
3. 旧 `メガボーマンダ` を `ボーマンダ` へ移行
4. 図鑑型セレクターを開く
5. 構築を6体まで補完
6. 保存
7. 育成スターター6枚を描画
8. 保存構築の1体を「未所持」にする
9. 未所持が保存構築・対戦準備用構築から即時除外されることを確認
10. 「所持に戻す」で未所持登録を解除
11. 相手6体を選択
12. 対戦準備分析を実行

結果: **PASS**

このテストにより、「未所持にしたのに基本戦術・対戦準備表示だけ古い」という表示不整合も防いでいます。

## 静的検証

**PASS**

確認内容:

- `pokemon-data.js` / `competitive-data.js` / `core.js` / `app.js` / `sw.js` 構文
- HTML重複IDなし
- `app.js` の固定DOM参照がすべてHTMLに存在
- `innerHTML` 直接代入なし
- `core.js` が二重読み込みされていない
- `competitive-data.js` 読み込みあり
- Service Worker `champion-coach-v6`
- Service Workerへ `competitive-data.js` を追加
- manifest JSON妥当
- 「持っていないポケモン」UIあり
- 「育成スターター」UIあり
- 「能力ポイント」「合計66」の案内あり
- アプリUI・主要ロジック内に旧来の数値表記・用語が残っていない

## データ根拠の分離

### ダブル

現行Reg M-Cの公開対戦データから技・持ち物・使用率を取得します。

公開データ:
https://eurekaffeine.github.io/pokemon-champions-scraper/battle_meta.json

スクレイパー:
https://github.com/eurekaffeine/pokemon-champions-scraper

オンライン更新が失敗しても使えるよう、2026-09-17時点の主要データを `competitive-data.js` に内蔵しています。

### シングル

公開スクレイパーのSingles詳細が旧Regulationを参照する状態を確認したため、v0.6では現行M-Cの根拠として流用していません。

このため、シングルで信頼できる現行データがない場合は、性格・能力ポイント・一般的な持ち物だけをスターターとして出し、技は断定しません。

### 性格・能力ポイント

公開使用率の数字から捏造していません。役割・基礎能力・トリックルーム等の既知役割から生成する「スターター配分」です。

能力ポイントの表記と実例はPokémon公式の記事でも `Stat Points` として提示されています。

https://www.pokemon.com/us/features/pokemon-champions-how-to-build-a-mega-malamar-team

## 検証中に発見・修正した問題

### 1. v0.5保存データが初期化される可能性

v0.6の初期実装では、保存データの移行処理がポケモンカタログ初期化より前に実行される順序があり、例外時にデフォルト状態へ戻る可能性がありました。

**修正:** カタログ初期化後に保存データを読み込む順序へ変更。v0.5形式データを事前投入した実行時テストを追加しました。

### 2. 未所持にした直後、保存済み分析表示だけ古く残る

構築本体から未所持ポケモンは外れても、基本戦術や対戦準備用構築の表示が次の全画面描画まで更新されない余地がありました。

**修正:** 保存済み構築の分析部分を独立再描画し、未所持登録直後に同期するよう変更しました。

### 3. 低速＝素早さ下降性格になりすぎる

初期ロジックでは、素早さが低い物理・特殊アタッカーへ広く「ゆうかん / れいせい」を出す可能性がありました。

**修正:** 素早さ下降性格は、トリックルーム役としての根拠がある場合に限定。耐久支援は防御系、通常の低速アタッカーはまず「いじっぱり / ひかえめ」とする安全側のスターターへ変更しました。

### 4. 旧保存データのフォーム矛盾

旧メガ表記・同種族別フォーム重複・未所持なのに構築内へ残る状態をインポートできる余地がありました。

**修正:** 読み込み時に現行カタログへ正規化し、同図鑑番号は1体へ整理、未所持との矛盾も除去します。

## 実ブラウザ自動描画について

過去版と同様、このコンテナのChromiumはDBus/プロセス終了の問題で安定したiPhone幅E2E撮影ができませんでした。成功したとは扱っていません。

代替として:

- JavaScript構文検査
- HTML/DOM静的整合検査
- 模擬DOMでの実行時操作
- 全件ロジックテスト
- 単体HTML生成後の一致検査
- 最終ZIP展開後の再検証

を完成条件にしています。

## 最終判定

ソース段階:

**100 / 100 PASS + STATIC PASS + RUNTIME SMOKE PASS + STANDALONE EXACT MATCH PASS**

配布候補ZIPを別フォルダへ展開し、展開後のコピーだけを使って次を再実行しました。

- `core.js` / `app.js` / `competitive-data.js` / `sw.js` 構文検査
- 100単体テスト
- 静的検査
- 実行時スモーク
- 単体HTMLと元4スクリプトの完全一致
- manifest JSON読込
- ZIP整合性検査

結果:

**100 / 100 PASS + STATIC PASS + RUNTIME SMOKE PASS + ZIP STANDALONE EXACT MATCH PASS + MANIFEST PASS + ZIP破損0件**

この結果を反映した `TEST_REPORT.md` を含む最終ZIPを再生成し、同じ検証をもう一度行って成果品を確定しています。


## v0.6.1 リポジトリ運用追加検証

### 追加した運用基盤

- GitHub Actions: 作業ブランチ/PR用 `validate.yml`
- GitHub Actions: main再検証 + Pages公開用 `pages.yml`
- `version.json` / `latest.json`
- GitHub Pages配布対象をruntimeファイルだけに限定
- Service Worker cache: `champion-coach-v6.1-repo1`
- navigation / index / manifest / versionはnetwork-firstへ変更
- JS/CSS等はcache-first + background refresh

### 再検証対象

- Node.js構文検査 5ファイル
- core/regression 100テスト
- runtime smoke test
- static consistency check
- GitHub Actions YAML構文
- `latest.json` に記録したruntime SHA256の一致
- standalone HTMLの最新版ソース再生成
- 最終ZIP展開後の再実行

GitHub Pagesの実デプロイ確認は、空リポジトリ作成とPagesの初回有効化後にmainへ投入して実施します。これはGitHub側の新規リポジトリ作成権限が現在の接続操作にないためで、アプリ本体・CI・Pages workflowの準備不足ではありません。
